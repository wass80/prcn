#include <bits/stdc++.h>
#define int long long
#define range(i,x,y) i=(x);i<(y);i++
typedef long long ll;
typedef long double ld;
using namespace std;

signed main(){
  int a,b; cin>>a>>b;
  cout << (b-1)/a + 1 << endl;
}
